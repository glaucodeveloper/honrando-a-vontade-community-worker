{
  "name": "honrando-a-vontade-community-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 migrations apply honrando-community --remote",
    "check": "node --check src/index.js"
  },
  "devDependencies": {
    "wrangler": "^4.131.2"
  }
}
